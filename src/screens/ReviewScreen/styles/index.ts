// index.ts
import { baseStyles } from './base';
import { histogramStyles } from './histogram';
import { reviewListStyles } from './reviewList';
import { modalFormStyles } from './modalForm';
import { modalFilterStyles } from './modalFilter';

export const styles = {
  ...baseStyles,
  ...histogramStyles,
  ...reviewListStyles,
  ...modalFormStyles,
  ...modalFilterStyles,
};
